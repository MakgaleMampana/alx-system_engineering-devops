#!/usr/bin/env bash
# This script is displaying time for 12 hours 59 min
hour=0
min=1

while [ "$hour" -le 12 ]
do
    echo "Hour: $hour"
    while [ "$min" -le 59 ]
    do
        echo "$min"
        ((min++))
    done
    min=1
    ((hour++))
done
